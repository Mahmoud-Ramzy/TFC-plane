name: Publish TFC Images to GHCR

on:
  push:
    branches:
      - tfc-branding

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    name: Build & Push ${{ matrix.name }}
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          # The web/admin/space/live images build from the repo root because their
          # Dockerfiles use `turbo prune` (they need the whole workspace + lockfile).
          - name: web
            context: .
            dockerfile: ./apps/web/Dockerfile.web
          - name: admin
            context: .
            dockerfile: ./apps/admin/Dockerfile.admin
          - name: space
            context: .
            dockerfile: ./apps/space/Dockerfile.space
          - name: live
            context: .
            dockerfile: ./apps/live/Dockerfile.live
          # api/proxy build from their own directory (dockerfile paths are
          # resolved relative to the repo root by buildx, not the context).
          - name: api
            context: ./apps/api
            dockerfile: ./apps/api/Dockerfile.api
          - name: proxy
            context: ./apps/proxy
            dockerfile: ./apps/proxy/Dockerfile.ce

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # GHCR/Docker image names must be lowercase; repository owners may have
      # uppercase characters (e.g. "Mahmoud-Ramzy"), so derive a lowercase prefix.
      - name: Compute lowercase image prefix
        id: prefix
        run: echo "value=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ${{ steps.prefix.outputs.value }}/tfc-plane-${{ matrix.name }}:latest
            ${{ steps.prefix.outputs.value }}/tfc-plane-${{ matrix.name }}:${{ github.sha }}
          cache-from: type=gha,scope=tfc-plane-${{ matrix.name }}
          cache-to: type=gha,mode=max,scope=tfc-plane-${{ matrix.name }}
